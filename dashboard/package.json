{
  "name": "ai-agent-tools-dashboard-tests",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "smoke": "playwright test",
    "smoke:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
