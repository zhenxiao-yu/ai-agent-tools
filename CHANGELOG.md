## 1.1.0 - 2026-05-07

### Features
- Restructure the Streamlit dashboard into modular config, data, services, page, and UI modules.

### Fixes
- Make dashboard paths resolve from the repository root instead of requiring `C:\ai-agent-tools`.
- Allow the repository allowlist to accept Python, Rust, Go, Java, Gradle, and generic versioned repos in addition to Node projects.

### Documentation
- Clarify the supported repository types in dashboard settings help text.
